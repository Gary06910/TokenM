'use strict';

module.exports = Object.freeze({
  cloudBaseEnvId: '',
  subscribeTemplateId: '',
  contactText: '',
  appVersion: '0.1.0',
  // Test-only adapter remains off unless a developer build explicitly opts in.
  enableUiMock: false,
  uiMockFixture: '',
});
